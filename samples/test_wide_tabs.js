function foobar(one,    two,    three_blabalbalba,      one) {
	// i    call    a       function        wheee!!
	if (true
			&& false
			|| NaN && false) {
		do_a_thing();
	} else {
		slack_off(with, your,   pals);
	}
	var a = [];
	//      head            tail,           floof
	a[1] = ["alpha",        "beta",         1];
	a[2] = ["gladiolusii",  "b",            3]
}