// after 4-to-8 tabs indentation conversion left margin of lines with
// &&s should stay 12 spaces wide

if (foo 
// <- 12  ->
            && bar
            && quux || zot ) {
    bring_it_on();
} else {
    slack_off();
}